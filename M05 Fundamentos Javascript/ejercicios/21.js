function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:
  return (x + y);
}

var resultado = suma(7, 2);
console.log("(1. Es mi console.log)", (resultado));


module.exports = suma;