function divide(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su división.
  // Tu código:
  return (x / y);
}

console.log("(1. Es mi consolelog)", divide(12, 3));
console.log("(2. Es mi consolelog)", divide(8, -2));
module.exports = divide;